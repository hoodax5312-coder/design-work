# 仓库协作规则

- 项目技术栈为 React + TypeScript；UI 规范入口为 `02-产品与设计/原始文档/ui-design-system.md`。
- 项目管理库入口为 `01-项目管理/README.md`；开始新任务前先读取 `01-项目管理/项目总览.md` 和 `01-项目管理/工作台.md`。
- 非技术负责人优先从项目说明与 README 了解项目；程序代码统一放在仓库根目录的 `src/`、`server/` 与 `scripts/`。本仓库已按标准 GitHub 项目结构整理，资料目录仅保留在本地并由 `.gitignore` 排除。
- 项目管理库只维护结构和已确认内容。识别到可能需要归档的文档、截图、聊天记录或结论时，先登记到 `01-项目管理/待确认资料.md` 并询问用户；未经明确同意，不复制、摘要或归档。
- 每个任务结束时，使用 `01-项目管理/_模板/任务.md` 留下任务记录；涉及方向、风险、决策、资料或复盘时，使用对应模板。
- 中文界面优先使用苹方（PingFang SC），回退到系统无衬线字体。
- 视觉基调使用中性黑、白、浅灰；语义色只用于明确的状态反馈。
- 每轮只修改用户指定的模块，不回退、覆盖或顺手整理无关修改。
- 修改前先阅读目标组件及 UI 规范；避免无目的遍历整个 `src`。
- 小型 UI 调整只运行 `npm run typecheck:client`。
- 模块完成后再运行 `npm run lint`；阶段完成后才运行测试和 `npm run build`。
- 不要在每个像素级调整后执行全量验证。
