# 更新日志

本项目所有正式版本变更记录在此文件中。版本号遵循语义化版本规范。

## [未发布]

### 调整

- 案例资源视图新增 All、GPT Image、Seedance、Nanobanana、Midjourney 五个模型 Tab，并使用 Remix 图标展示模型来源。

- 主导航新增独立“案例”入口并置于“生成”之前；原“资产”入口改名为“素材”，词库与笔记收拢到资产工作区，主导航移除“知识”入口并保留旧路由兼容。

- 画布项目表情入口在展开与收起状态均统一为 20px 全圆角头像，保留 32px 悬停/点击热区与主题浅灰背景。
- 画布项目入口页标题统一使用 20px 字号与 24px 顶部留白，与工具页、设置页的顶部标题规范保持一致；搜索框布局不变。
- 电商工作区左侧“素材输入 / 参数配置”标签按钮统一调整为 36px 高，保留两列布局、选中态与切换交互。
- 电商工作区“KV 视觉系统 / 识别报告 / 详情页拼图”三个顶部标签按钮统一调整为 32px 高；40px 工具栏容器和切换交互保持不变。
- 图片生成页保持左右栏共用同一卡片的拼接结构，仅将右侧生成结果模块的左内边距调整为 12px；左侧参数栏和生成交互保持不变。
- 画布项目搜索框改为复用知识模块的 `module-search-input` 规范及中性表面、描边和聚焦变量；保留 240px 桌面宽度、32px 高度与既有搜索行为。
- Default 浅色模式的数据存储“保存位置 / 缓存目录”输入框移除默认可见描边，聚焦时继续显示中性浅描边；暗黑模式和其他主题不受影响。
- Default 主题下知识模块“已打开的知识”标签栏底部分割线改为左右各缩进 16px，与正文内容边缘对齐；其他主题保持通栏分割线。
- 知识模块“已打开的知识”标签栏前新增文件树收起/展开按钮，沿用现有 Remix 侧栏图标与 15px 尺寸；标签栏继续保持独立的可访问 `tablist` 语义。
- 助手库标题前新增分类侧栏收起/展开按钮，并与资产模块统一为 28px 按钮热区、15px Remix 图标和悬停背景；“从外部导入”“管理助手”“创建助手”三个操作图标统一调整为 14px。
- 知识文件树管理模式的选择统计栏与词库标签管理保持一致：使用 36px 高度、底部分割线、左侧全选框、中间选择计数和右侧 28px 删除按钮；项目节点不参与全选。
- 词库标签管理顶部移除单项编辑按钮，仅保留选择统计与批量删除；每个标签行默认显示数量，hover 或键盘聚焦时切换为编辑和删除图标，不产生布局跳动。
- 词库标签管理行将拖拽手柄移到最左侧、多选框移到最右侧，标签名称与数量保持在中间；拖拽排序和多选交互不变。
- 知识文件树管理模式的条目选择控件由 24px 收紧为 16px，内部勾选图标同步调整为 10px；选择逻辑与行高保持不变。
- Default 浅色模式的助手编辑名称与提示词字段移除默认轻投影，继续保留浅灰底、透明描边以及聚焦后的白底浅描边。
- Default 浅色模式的资产、知识与画布项目搜索框统一移除默认阴影，聚焦时显示中性浅描边；画布项目搜索框高度同步调整为 32px。
- 知识中心顶部搜索框在获得焦点后隐藏占位提示文字，进入清晰的输入状态；失焦且内容为空时恢复提示，搜索与键盘交互保持不变。
- 知识模块的“新建标签 / 管理标签”与“新建笔记 / 新建文件夹 / 管理文件”顶部操作组按钮间距统一为 12px，按钮尺寸与交互保持不变。
- 词库顶部“管理助手”按钮改用 Remix `Equalizer3` 图标，20px 尺寸、按钮样式与管理交互保持不变。
- 知识文件树的“管理文件”与词库分类的“管理标签”入口统一改用 Remix `Equalizer2` 图标，按钮尺寸、状态样式与管理交互保持不变。
- 助手创建/编辑弹窗移除标题下方的说明文字，标题栏固定为 48px 高，关闭按钮同步垂直居中；表单与操作区保持不变。
- Default 浅色模式的助手编辑弹窗名称输入框与提示词文本框曾统一为默认浅灰底、透明描边和轻投影；默认轻投影已被后续无阴影调整替代，聚焦白底与中性浅描边继续保留。
- 知识模块助手库的左侧分类标签补充右键操作，支持查看筛选、新建、编辑和删除；“全部”保持不可编辑与删除，删除普通标签时会将关联助手迁移到保留标签；操作菜单调整为 240px 宽、44px 行高和 14px 文字，提升可读性与点击面积。
- 知识文件树管理模式将文件夹展开与条目选择拆分为独立操作：左侧内容区负责展开/收起，右侧勾选按钮负责选择；根文件夹拖动手柄移至最左侧，并统一为 8px 区域间距。
- 画布收起状态的项目表情入口调整为 24px 表情，并补充主题浅灰背景；32px 点击区域与项目切换交互保持不变。
- 画布右侧 Mini Map 与 Agent 面板改为双向互斥：打开其中一个会自动关闭另一个，避免右侧工作区同时堆叠两个浮层。
- 画布视频节点提示工具栏的模型选择器固定为 160px 宽，右侧比例控件调整为 28×28px 正方形；图片节点模型选择仍保持自适应宽度。
- 画布底部悬浮工具栏的主操作图标统一调整为 16px；下拉箭头与弹出菜单内的辅助图标保持原尺寸。
- 资产模块素材标签筛选的首项实际顶部间距调整为 12px，移除类型分组额外叠加的 4px 顶部内边距。
- 知识模块词库的助手分类导航顶部间距由 4px 调整为 12px，左右与底部间距保持不变。
- 画布项目入口页标题右侧的搜索框在桌面端固定为 `240px` 宽，窄屏继续使用可用宽度。
- 知识模块的笔记文件树在管理模式下支持通过拖拽手柄调整根文件夹顺序；落点提示区分目标前后，结果复用现有文件树本地存储，笔记与项目节点不参与此排序。
- 知识模块词库的标签管理模式支持多选标签、单项重命名、批量删除和拖拽排序；“全部”保持固定，删除时至少保留一个标签并将关联助手迁移到保留标签。
- Default 暗黑模式的模块工作区背景由 `lab(5.26802%)` 小幅提亮为 `lab(7%)`，增强资产等内容区域与黑色应用底层的层级区分；浅色模式和其他主题不受影响。
- Default 浅色模式的知识与资产顶部搜索框曾与画布项目搜索框统一为浅灰底、透明边框和轻投影；该投影结论已被后续“默认无阴影、聚焦显示描边”调整替代。
- Default 浅色模式的数据存储内容卡片与个性化设置分组统一，移除外层描边并使用同一套低透明度轻投影；其他主题保持各自面板样式。
- 知识笔记富文本工具栏高度收紧为 36px，格式操作按钮统一调整为 28×28px，编辑能力保持不变。
- 知识模块笔记文件树的三个顶部操作图标改为整体居中，并通过主题分割线与下方树结构分隔。
- 知识模块的笔记正文接入现有 TipTap 富文本编辑器，顶部新增粗体、斜体、删除线、二级标题、列表、引用、行内代码及撤销/重做工具栏；图标继续使用 Remix Icon，旧笔记内容自动兼容并按原路径本地持久化。
- 知识模块词库左侧顶部操作改为“新建标签 / 管理标签”，与右侧助手内容管理彻底分离；支持标签新建、重命名、删除、分类迁移及本地持久化，顶部分别使用 Remix 标签与编辑图标，居中排列并通过主题分割线与标签列表分隔。
- 工具页与设置页标题增加画布式辅助说明，统一页面标题的信息层级、次级文字样式和间距。
- Claude 模式下的设置页分段控件与字体选择框统一使用 `--neutral-border` 的 1px 中性浅描边，导航栏位置、样式、收起方式、字体与语言控件保持一致。
- 画布项目入口页的搜索框移动到标题栏右侧，桌面端与标题说明底部对齐，窄屏自动换行并使用可用宽度。
- Default 浅色模式的通用内容卡片移除描边，统一使用低透明度轻投影和克制的悬停投影；画布项目搜索框同步移除可见描边，暗黑模式和其他主题不受影响。
- 浅色模式下的字体选择框移除可见描边，保留原有底色、尺寸与聚焦反馈；其他主题和共享选择框不受影响。
- 浅色模式的设置分组移除外层描边，改用低透明度轻投影；按钮描边、组内分隔线和其他主题保持不变。
- 图片生成页的输入分区、结果列表与生成记录头部统一使用内容工作区背景变量，移除多余的卡片底色层级。
- 左右导航的 64px 窄条收起态改为继续显示图标和文字，沿用图标在上、文字在下的紧凑布局；200px 宽条与顶部导航的文字显示保持不变。
- 完全隐藏的导航在边缘悬浮显示时提高临时预览层级，避免被工作区顶部控件遮挡；边缘感应条只负责打开，鼠标真正离开导航浮层后才延迟收回。
- 顶部导航在非完全隐藏状态下固定显示图标和文字，与左右导航窄条的文字保留规则一致。
- 顶部导航项按钮高度统一为 32px，图标和文字保持横向居中。
- 设置页左侧分类导航与右侧内容之间恢复使用主题面板变量的竖向分割线。
- 导航位置切换到左侧或右侧时恢复展开状态，确保完整显示图标和文字；之后仍可收起为图标加文字的窄条或完全隐藏。
- 顶部导航的六个功能按钮整组右对齐，品牌区继续固定在左侧。
- 唯一 Default 主题改用原 Vercel 的中性黑白灰亮色/暗色 token、Geist 字体和轻量卡片表面处理，三种主题模式切换逻辑保持不变。
- 浅色、暗色、跟随系统三个模式卡片恢复完整界面缩略预览、底部名称和选中勾选，替代仅显示图标与文字的极简样式。
- Vercel 工作区新增独立模块表面 token：模块背景比底部应用外壳更浅，修复两层背景共用同一灰度的问题，并同步提供暗色层级。
- 生成操作色移除遗留黄色硬编码并接入主题语义色：亮色主按钮为黑底白字，暗色反转，相关弱选中态统一使用中性灰。
- 主题模式下方新增“更多风格”三列区域，恢复 Neo-brutalism 与 Claude 两个可持久化主题入口，第三列留空；点击首排模式卡片会返回 Vercel Default 对应模式。
- 个性化设置导航术语统一为“导航栏样式”和“导航栏收起”，可见文案与控件无障碍标签保持一致。
- 主题模式与更多风格卡片改为全尺寸缩略图，名称、图标和选中勾选使用底部半透明蒙层覆盖，移除独立白色名称底栏。
- 主题卡片蒙层改为从全透明到 50% 黑色的纵向渐变，降低底部遮挡并保留白色文字可读性。
- 个性化设置新增“导航栏位置”，支持左侧、顶部、右侧并持久化；三个位置均支持保留图标或完全隐藏。
- 导航完全隐藏后，鼠标靠近对应屏幕边缘会临时浮层展开导航，顶部位置显示完整横向导航；移出后自动收回，且不改写用户的隐藏偏好。
- 导航完全隐藏时同步移除容器的边框、背景、圆角、阴影和外边距，避免浮动导航在屏幕边缘残留折叠线。
- 浮动导航样式在左侧、顶部、右侧三种位置下均与工作区保持 8px 间距。
- 五张主题卡片改为单选；Neo-brutalism 与 Claude 固定使用各自的浅色外观，切换与持久化恢复时都会纠正从 Vercel 继承的暗色状态。
- 主题设置恢复为单一 Default 主题体系，仅保留浅色、暗色、跟随系统三个卡片模式；历史主题预设统一迁移到 `nature`，不再提供多主题入口。
- 主题设置收敛为 Default、Neo-brutalism、Claude 三个入口；Default 支持三种主题模式，其他主题固定浅色并迁移旧主题值。

- 正式产品名称由 Design Work 更新为栗作 LIZUO，同步主界面、用户提示和项目文档。
- 保留 `design-work`、`.design-work` 和旧默认存储路径作为兼容性技术标识。
- 第五个预设主题由 Tangerine 替换为 Terminal，采用深色终端背景、荧光绿主色、琥珀辅助色、1px 直角无阴影边界、等宽字体和轻微 scanline；历史 `tangerine` 偏好自动迁移。
- 第二个预设主题更名为 Neo-brutalism，按参考站恢复奶油底、红黄紫色板、4px 黑色描边、无圆角和零模糊硬阴影；内部键 `brutalist` 保持兼容。
- 内嵌模式工作区保留上下与右侧外壳间距，仅移除外层描边；普通分割与浮动模式的外壳行为保持不变。
- 设置页内层工作区改用主题面板变量，保留内嵌外壳间距；Neo-brutalism 使用 4px 直角粗描边，其他主题沿用各自风格。
- 资产库外壳、筛选栏分隔线和内容区标题分割线改用主题面板描边变量。
- 工作区浮动外壳的圆角、描边和阴影改用主题面板变量，内嵌外壳继续保留间距并隐藏外层描边。
- 助手库外壳、分隔线和助手卡片改用主题面板与卡片变量。
- 主题预设卡片统一外部容器样式，预设风格差异保留在内部预览。
- 资产库工作区根容器补充主题面板阴影变量，确保外壳投影随主题生效。

## [1.1.1] - 2026-08-03

### 新增

- 小记标签创建、筛选和标签管理能力。
- 画布工具栏与项目交互优化。
- 视频工程素材分类和工程详情视图。
- 文档模块整合与工作区界面调整。

### 调整

- 统一主要模块的导航、内容边距、搜索框和卡片视觉。
- 优化小记输入、列表、标签弹层与卡片展示。
- 精简视频工程中的重复标题和说明文案。
