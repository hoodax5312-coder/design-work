# 任务：中文目录彻底迁移

- 日期：2026-08-04
- 状态：#已完成
- 任务类型：工程目录迁移 / 项目管理可读性
- 目标：让非技术负责人打开项目时，优先看到中文分区，同时保持程序可运行。

## 迁移结果

| 新目录 | 内容 |
| --- | --- |
| `00-老板入口/` | 老板驾驶舱和目录导航 |
| `01-项目管理/` | 项目计划、任务、决策、风险、版本和复盘 |
| `02-产品与设计/` | 原始产品文档和设计探索 |
| `03-程序区/` | 前端代码、后端服务、启动构建工具 |
| `04-质量与验收/` | 视觉审计和验收证据 |

## 配置同步

- `package.json`：更新启动、测试、构建和基准命令。
- `vite.config.ts`：更新后端 API 入口和前端别名。
- `tsconfig.json`、`tsconfig.server.json`：更新源码目录。
- `tailwind.config.js`、`components.json`、`index.html`：更新前端目录。
- 启动/构建脚本：更新根目录计算和服务入口。
- `AGENTS.md` 和老板导航：更新新目录路径。

## 验证

- `npm run typecheck`：通过。
- `npm test`：42/42 通过。
- `npm run build`：通过。
- 构建仍有主 JS 包体积约 707KB 的既有性能提示，不是本次迁移引入的失败。

## 未执行

- 未执行 GitHub push、创建 tag、Release 或 Pages 发布。
- 未删除 `.design-work`、`.vercel`、`.gstack`、`node_modules`、`dist`、`output` 等运行/生成目录。
