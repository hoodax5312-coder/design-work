# 任务：创作历史缓存持久化

- 状态：#已完成
- 负责人：Codex
- 优先级：P1
- 创建日期：2026-08-07

## 目标

图像生成、视频生成和无限画布的工作状态写入用户设置的缓存目录，模块切换与页面刷新后能够恢复。

## 范围

- 图像与视频生成记录。
- 无限画布的画布列表、活动画布、节点、连线及 Agent 对话。
- 旧生成清单向新缓存历史的兼容迁移。

## 不在范围内

- 素材正式数据仍沿用数据目录与资产数据库。
- 不改变用户当前设置的缓存目录。

## 验收标准

- [x] 生成历史写入缓存目录的 `workspace/generation-history.json`。
- [x] 画布状态写入缓存目录的 `workspace/canvas-workspace.json`。
- [x] 图像/视频/画布相互切换后状态仍存在。
- [x] 页面重新进入后可从缓存恢复。
- [x] 旧图片和视频生成记录自动迁移并显示。

## 记录

- 当前缓存目录：`/Users/hoodax/Documents/Design Work/工作台缓存`。
- 已实测恢复 2 条图片生成记录、1 条视频生成记录。
- 已实测添加画布文本节点后切换到图像再切回，节点仍存在。
- `npm run typecheck` 与 `npm run lint` 通过。

## 交接与下一步

- 素材正式数据与创作缓存现在按用户理解分开存储。
