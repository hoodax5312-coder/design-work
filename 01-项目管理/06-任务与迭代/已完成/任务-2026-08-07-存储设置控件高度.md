# 任务：统一存储设置输入框与选择按钮高度

- 状态：#已完成
- 负责人：Codex
- 优先级：P2
- 创建日期：2026-08-07

## 目标

将存储设置中目录输入框与“选择文件夹”按钮统一为 36px 高度。

## 范围

- `03-程序区/前端代码/components/modals/settings/StorageSettings.tsx`

## 不在范围内

- 不修改全局 Input、Button 组件及其他模块尺寸。

## 验收标准

- [x] 数据目录输入框与选择按钮均为 36px。
- [x] 缓存目录输入框与选择按钮均为 36px。
- [x] 客户端 TypeScript 类型检查通过。

## 记录

- 为两组目录控件显式添加 `h-9`（36px）类名，避免受默认输入框 40px 与按钮尺寸影响。
- 已运行 `npm run typecheck:client`，通过。

## 交接与下一步

- 可在存储设置页面确认两组控件视觉对齐。
